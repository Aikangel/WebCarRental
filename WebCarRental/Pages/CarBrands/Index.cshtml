@page
@model WebCarRental.Pages.CarBrands.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CarBrand[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CarBrand[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CarBrand[0].TechnicalSpecifications)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CarBrand) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TechnicalSpecifications)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.BrandCode">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.BrandCode">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.BrandCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
